# Freqtrade 策略开发深度指南

> 基于对 freqtrade 源码（`freqtrade/strategy/interface.py`、`freqtrade/optimize/backtesting.py`、`freqtrade/freqtradebot.py`、`freqtrade/exchange/exchange.py`、`freqtrade/data/dataprovider.py`）的逐行研读，从架构、数据链路、策略 API、回测机制到实盘运行，完整拆解。

---

## 目录

1. [框架架构全景图](#一框架架构全景图)
2. [策略基类 IStrategy 完整 API](#二策略基类-istrategy-完整-api)
2.5. [声明式信号→命令式订单：源码级信号链路](#二点五声明式信号--命令式订单源码级信号链路)
3. [数据链路深度拆解](#三数据链路深度拆解)
4. [回测机制详解](#四回测机制详解)
5. [实盘/模拟盘机制详解](#五实盘模拟盘机制详解)
6. [回测 vs 实盘对比](#六回测-vs-实盘对比)
7. [从零开始写策略](#七从零开始写策略)

---

## 一、框架架构全景图

### 1.1 三层架构

```
┌──────────────────────────────────────────────────────┐
│                     用户层                           │
│  config.json  │  user_data/strategies/*.py           │
│  freqtrade CLI (backtesting / trade / webserver)    │
└────────────────────────┬─────────────────────────────┘
                         │
┌────────────────────────┴─────────────────────────────┐
│                    核心引擎层                         │
│                                                     │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────┐  │
│  │   Worker     │  │ FreqtradeBot │  │Backtesting │  │
│  │  (主循环)    │  │ (实盘引擎)   │  │(回测引擎)  │  │
│  └──────┬───────┘  └──────┬───────┘  └─────┬──────┘  │
│         │                 │               │         │
│  ┌──────┴─────────────────┴───────────────┴──────┐  │
│  │              Strategy (IStrategy)             │  │
│  │  populate_indicators / entry / exit / custom  │  │
│  └──────────────────────┬────────────────────────┘  │
└─────────────────────────┼───────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────┐
│                    数据层                            │
│                                                     │
│  ┌───────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Exchange  │  │ DataProvider │  │ DataHandlers │  │
│  │           │  │              │  │              │  │
│  │ _api(sync)│  │ 数据缓存+    │  │ .feather     │  │
│  │ _api_async│  │ 访问接口     │  │ .json .parq  │  │
│  │ _ws_async │  │              │  │              │  │
│  │ _klines   │  │              │  │              │  │
│  └───────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 1.2 核心类关系详解

```
Configuration  ← 配置文件解析
    │
    ▼
Worker  ← 主循环（状态机: RUNNING / STOPPED / PAUSED）
    │
    ├── Exchange  ← 交易所抽象层
    │     ├── _api         (同步 ccxt)    — 下单、取消、查询余额
    │     ├── _api_async   (异步 ccxt.pro) — K线、市场数据
    │     ├── _ws_async    (WebSocket)    — 实时行情推送（可选）
    │     └── _klines      内存缓存: {(pair, tf, type): DataFrame}
    │
    ├── FreqtradeBot  ← 交易引擎
    │     ├── process()              → 主处理循环（每个 tick 一次）
    │     │   ├── reload_markets()   → 刷新市场数据
    │     │   ├── dataprovider.refresh() → 拉取最新K线
    │     │   ├── strategy.analyze() → 跑策略指标和信号
    │     │   ├── manage_open_orders() → 管理未成交订单
    │     │   ├── exit_positions()   → 检查离场条件
    │     │   └── enter_positions()  → 检查入场信号
    │     ├── create_trade()         → 开仓
    │     ├── execute_entry()        → 执行入场订单
    │     ├── handle_trade()         → 管理持仓
    │     └── execute_trade_exit()   → 执行离场订单
    │
    ├── Strategy  ← 你写的策略
    │     ├── populate_indicators()  → 计算指标
    │     ├── populate_entry_trend() → 入场信号
    │     ├── populate_exit_trend()  → 离场信号
    │     ├── custom_exit()          → 自定义离场
    │     ├── custom_stoploss()      → 动态止损
    │     ├── adjust_trade_position() → DCA/马丁
    │     └── 30+ 个可覆盖的回调...
    │
    ├── DataProvider  ← 策略访问数据的唯一入口
    │     ├── get_analyzed_dataframe() → 已计算指标的K线
    │     ├── ohlcv()                  → 原始OHLCV
    │     ├── current_whitelist()      → 当前交易对
    │     └── _set_cached_df()         → 缓存已分析的DataFrame
    │
    ├── RPCManager  ← 消息分发
    │     ├── Telegram     (rpc/telegram.py)
    │     └── API Server   (FreqUI Web界面)
    │
    └── Wallets  ← 钱包管理
          ├── 实盘: 调用 exchange.get_balances()
          └── 模拟: 从数据库计算 start_cap + 盈亏 - 占用
```

### 1.3 Exchange 双客户端设计

freqtrade 维护两个独立的 ccxt 客户端实例：

| 客户端 | 类型 | 用途 |
|--------|------|------|
| `self._api` | 同步 `ccxt.Exchange` | 下单、取消、查余额、获取订单簿 |
| `self._api_async` | 异步 `ccxt.pro` / `ccxt.async_support` | K线数据、市场信息加载 |
| `self._ws_async` | 异步（第三个实例） | WebSocket 实时行情（可选） |

**为什么分开？**
- K线获取频繁且需要并行 → 用异步 API
- 下单是低频交互 → 用同步 API 更简单
- WebSocket 为实时推送 → 独立实例

异步操作在 Exchange 内部专用的 `asyncio.AbstractEventLoop` 上运行，通过 `self._loop_lock` 串行化访问。

---

## 二、策略基类 IStrategy 完整 API

### 2.1 类层次

```
HyperStrategyMixin (hyper.py)  ← 超参优化混入（IntParameter / RealParameter 等）
    │
    └── IStrategy (ABC) (interface.py)  ← 你继承这个
```

### 2.2 三个核心方法（必须实现）

#### `populate_indicators(dataframe, metadata) → DataFrame`

| 项目 | 说明 |
|------|------|
| **调用时机** | 每次处理K线时（回测和实盘都调） |
| **输入** | `dataframe`: OHLCV 列 (`date, open, high, low, close, volume`) + 所有 `@informative` 装饰器计算出的列 |
| **输入** | `metadata`: `{"pair": "BTC/USDT"}` |
| **输出** | 添加了技术指标列的 DataFrame |
| **注意** | 只能加列，不能删列，不能修改 OHLCV 本身 |

**两个模式的差异：**
- **回测**：`advise_all_indicators()` 一次性对所有历史K线调用
- **实盘**：`analyze_pair() → _analyze_ticker_internal() → analyze_ticker() → advise_indicators()` 每新K线调用

#### `populate_entry_trend(dataframe, metadata) → DataFrame`

必须设置：
- `enter_long` = `1`（做多入场），`0` 表示不操作
- `enter_short` = `1`（做空入场）
- `enter_tag` = `"信号名"`（可选，用于统计分类）

**信号流：** 策略设置了 `enter_long` 列后，实盘中 `FreqtradeBot.create_trade()` 调用 `strategy.get_entry_signal()` 读取最后一行的 `enter_long` 列。

#### `populate_exit_trend(dataframe, metadata) → DataFrame`

必须设置：
- `exit_long` = `1`（平多）
- `exit_short` = `1`（平空）

**旧版兼容：** 如果你用了 `buy` / `sell` / `buy_tag` 列名，freqtrade 会自动重命名为 `enter_long` / `exit_long` / `enter_tag`。

### 2.3 全部可覆盖回调（按执行顺序）

#### 生命周期回调

| 方法 | 调用时机 | 回测 | 实盘 |
|------|---------|:--:|:--:|
| `bot_start(**kwargs)` | 启动时一次 | ❌ | ✅ |
| `bot_loop_start(current_time, **kwargs)` | 每个主循环迭代开始时 | ❌ | ✅ |

#### 信号生成（核心三件套）

| 方法 | 调用时机 | 所有模式 |
|------|---------|:--:|
| `populate_indicators(df, meta)` | 每次K线更新 | ✅ |
| `populate_entry_trend(df, meta)` | populate_indicators 之后 | ✅ |
| `populate_exit_trend(df, meta)` | populate_entry_trend 之后 | ✅ |

#### 入场控制

| 回调 | 签名 | 返回值 | 作用 |
|------|------|--------|------|
| `custom_entry_price` | `(pair, trade, time, proposed_rate, entry_tag, side, **kw)` | `float` | 覆盖入场价格 |
| `custom_stake_amount` | `(pair, time, rate, proposed_stake, min_stake, max_stake, leverage, entry_tag, side, **kw)` | `float` | 自定义投入金额（Kelly/波动率） |
| `confirm_trade_entry` | `(pair, order_type, amount, rate, time_in_force, time, entry_tag, side, **kw)` | `bool` | **最后准入控制**—True=允许成交 |
| `leverage` | `(pair, time, rate, proposed, max, entry_tag, side, **kw)` | `float` | 自定义杠杆（仅期货） |

> ⚠️ `confirm_trade_entry` 在回测中也调用，但可以自定义逻辑来区分模式。

#### 离场控制

| 回调 | 返回值 | 优先级 |
|------|--------|:--:|
| `custom_exit(pair, trade, time, rate, profit, **kw)` | `str` = 退出原因，`None` = 不操作 | ④ |
| `custom_stoploss(pair, trade, time, rate, profit, after_fill, **kw)` | `float`（负数比率） | ② |
| `custom_exit_price(pair, trade, time, proposed_rate, profit, exit_tag, **kw)` | `float` | — |
| `confirm_trade_exit(pair, trade, order_type, amount, rate, time_in_force, exit_reason, time, **kw)` | `bool` | — |

**离场优先级（由高到低，`should_exit()` 中的执行顺序）：**

```
1. stoploss (硬止损)           — 最高，不可跳过
2. custom_stoploss() 返回值    — 动态止损（且不能比 stoploss 更宽松）
3. trailing_stop              — 移动止损
4. minimal_roi                — 时间+利润率表
5. custom_exit() / exit_signal — 自定义/策略信号
```

#### 仓位调整

| 回调 | 签名 | 返回值 |
|------|------|--------|
| `adjust_trade_position` | `(trade, time, rate, profit, min_stake, max_stake, entry_rate, exit_rate, entry_profit, exit_profit, **kw)` | `float > 0` = 加仓, `< 0` = 减仓, `None` = 不操作 |

**使用前置条件：** 必须设置 `position_adjustment_enable = True`。

#### 订单管理

| 回调 | 调用时机 | 返回值 |
|------|---------|--------|
| `adjust_entry_price` | 入场订单未成交，每个K线调用 | `float` = 新价格, `None` = 取消 |
| `adjust_exit_price` | 离场订单未成交，每个K线调用 | 同上 |
| `check_entry_timeout` | 入场订单超时检查 | `True` = 取消订单 |
| `check_exit_timeout` | 离场订单超时检查 | `True` = 取消订单 |
| `order_filled` | 任何订单成交后 | 无返回值（通知用） |

#### 信息型装饰器

```python
from freqtrade.strategy import informative

class MyStrategy(IStrategy):
    @informative('1h')                     # 同交易对的1h数据
    def populate_indicators_1h(self, dataframe, metadata):
        dataframe['rsi_1h'] = ta.RSI(dataframe, timeperiod=14)
        return dataframe

    @informative('4h', 'BTC/USDT')         # BTC 的 4h 数据
    def populate_indicators_btc(self, dataframe, metadata):
        dataframe['btc_trend'] = dataframe['close'] > ta.SMA(dataframe, 50)
        return dataframe
```

- 装饰器方法在主 `populate_indicators` **之前**被调用
- 列会自动合并到主 DataFrame，避免 lookahead bias
- **限制：** 信息型时间周期必须 ≥ 策略时间周期

### 2.4 策略类属性完整清单

```python
class MyStrategy(IStrategy):
    # ── 必设 ──
    timeframe = '5m'               # K线周期
    can_short = False              # 是否允许做空

    # ── 止盈止损 ──
    minimal_roi = {                # 时间→最低盈利率
        "60": 0.01,                # 60分钟盈利1%就卖
        "30": 0.02,                # 30分钟盈利2%就卖
        "0": 0.05                  # 立即盈利5%就卖
    }
    stoploss = -0.10               # 硬止损 -10%

    # ── 可选 ──
    trailing_stop = False           # 移动止损
    trailing_stop_positive = 0.005  # 盈利0.5%后启动移动止损
    trailing_stop_positive_offset = 0.0051
    trailing_only_offset_is_reached = False

    process_only_new_candles = True # 只在新K线时处理（推荐）
    use_exit_signal = True          # 使用populate_exit_trend信号
    exit_profit_only = False        # 只在盈利时卖出
    ignore_roi_if_entry_signal = False

    # ── 仓位调整 ──
    position_adjustment_enable = False
    max_entry_position_adjustment = -1  # -1=无限

    # ── 启动所需历史K线数 ──
    startup_candle_count = 200      # EMA200需要至少200根

    # ── 订单类型 ──
    order_types = {
        'entry': 'limit',
        'exit': 'limit',
        'stoploss': 'market',
        'stoploss_on_exchange': False,
    }
    order_time_in_force = {
        'entry': 'GTC',   # GTC / IOC / FOK
        'exit': 'GTC',
    }

    # ── 保护 ──
    protections = []               # StoplossGuard, MaxDrawdown 等
```

### 2.5 超参优化参数

```python
from freqtrade.strategy import IntParameter, RealParameter, CategoricalParameter

class MyStrategy(IStrategy):
    buy_rsi = IntParameter(10, 40, default=30, space='buy')
    sell_rsi = IntParameter(60, 90, default=70, space='sell')
    ema_ratio = DecimalParameter(0.8, 1.2, default=1.0, decimals=2, space='buy')
    indicator = CategoricalParameter(['RSI', 'MACD', 'BB'], default='RSI', space='buy')

    def populate_indicators(self, dataframe, metadata):
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=self.buy_rsi.value)
        return dataframe
```

**参数类型：**

| 类型 | 值范围 | 示例 |
|------|--------|------|
| `IntParameter(low, high)` | 整数 | `IntParameter(5, 50, default=12)` |
| `RealParameter(low, high)` | 浮点（无限精度） | `RealParameter(0.01, 0.10, default=0.05)` |
| `DecimalParameter(low, high, decimals=N)` | 浮点（N位小数） | `DecimalParameter(0.8, 1.2, decimals=2)` |
| `CategoricalParameter(categories)` | 枚举 | `CategoricalParameter(["a", "b"], default="a")` |
| `BooleanParameter()` | True/False | `BooleanParameter(default=True)` |

**参数加载优先级：** 默认值 → JSON超参结果文件 → `{space}_params` 字典

---

## 二点五、声明式信号 → 命令式订单：源码级信号链路

这是 freqtrade 最核心也最容易让人困惑的设计：**你只打标记，框架来下单**。

### 为什么你不需要调用 buy() 和 sell()

你的策略只负责在两处打标：

```python
dataframe.loc[condition, 'enter_long'] = 1    # 声明：这里应该做多
dataframe.loc[condition, 'exit_long'] = 1     # 声明：这里应该平多
```

框架在 `FreqtradeBot` 中读这些标记，自己决定是否下单、怎么下单。这叫**声明式策略**，和你之前写的"命令式策略"有本质区别。

### 入场信号 → 订单的完整代码链

以下是 `enter_long = 1` 从 DataFrame 列变成交易所订单的完整路径：

```
═══════════════════════════════════════════════════
第 1 步：你打标记（你的策略代码）
═══════════════════════════════════════════════════

  dataframe.loc[condition, 'enter_long'] = 1

═══════════════════════════════════════════════════
第 2 步：框架读取最后一行（interface.py:1344）
═══════════════════════════════════════════════════

  # FreqtradeBot.create_trade() 调用：
  signal, enter_tag = self.strategy.get_entry_signal(pair, timeframe, analyzed_df)

  # get_entry_signal() 内部逻辑：
  latest = analyzed_df.iloc[-1]                    # ← 只看最后一行！
  enter_long = latest.get('enter_long', 0) == 1   # ← 读你打的标
  exit_long  = latest.get('exit_long', 0) == 1    # ← 检查有没有冲突

  # 防冲突：如果同时有 exit_long 或 enter_short，做多信号被忽略
  if enter_long == 1 and not any([exit_long, enter_short]):
      return SignalDirection.LONG, enter_tag     # ← 返回"做多"

  # 做空同理（需要 can_short=True 且非现货模式）
  if enter_short == 1 and not any([exit_short, enter_long]):
      return SignalDirection.SHORT, enter_tag    # ← 返回"做空"

═══════════════════════════════════════════════════
第 3 步：框架决定是否开仓（freqtradebot.py:684）
═══════════════════════════════════════════════════

  # create_trade() 拿到 SignalDirection.LONG 后，做一系列检查：
  if signal:
      # ① 还有空余仓位吗？（max_open_trades 限制）
      if not self.get_free_open_trades():
          return False

      # ② 交易对被锁定了吗？（ProtectionManager）
      if self.strategy.is_pair_locked(pair):
          return False

      # ③ 市场深度够吗？（可选配置）
      if depth_of_market_check_fails:
          return False

      # ④ 计算投入金额
      stake_amount = self.wallets.get_trade_stake_amount(pair, max_trades)
      # → 基于 config 中的 stake_amount、tradable_balance_ratio 计算

      # ⑤ 进入执行
      self.execute_entry(pair, stake_amount, ...)

═══════════════════════════════════════════════════
第 4 步：框架执行订单（freqtradebot.py:895）
═══════════════════════════════════════════════════

  # execute_entry() 内部：
  side = "sell" if is_short else "buy"           # ← 决定买卖方向！
  name = "Short" if is_short else "Long"         # ← 日志用的名字

  # ① 确定价格
  enter_price, stake_amount, leverage = self.get_valid_enter_price_and_stake(...)
  # → 调 exchange.get_rate()、custom_entry_price() 回调

  # ② 计算数量
  amount = (stake_amount / enter_price) * leverage

  # ③ 准入确认（你可以在这里拦截）
  if not strategy.confirm_trade_entry(pair, order_type, amount, rate, ...):
      return False  # ← 策略说不行，放弃这笔交易

  # ④ 实际下单！
  order = self.exchange.create_order(
      pair=pair,
      ordertype="limit",   # 或 "market"，来自 order_types 配置
      side=side,           # ← "buy" 或 "sell"
      amount=amount,
      rate=enter_price,
  )
  # dry_run→模拟订单，live→真实 ccxt.create_order()

  # ⑤ 创建数据库记录
  Trade.create(...)     # 新交易记录
  Order.create(...)     # 订单记录

  # ⑥ 通知
  rpc.send_msg({'type': 'entry', ...})  # → Telegram + WebUI
```

### 离场信号 → 平仓订单的代码链

```
═══════════════════════════════════════════════════
第 1 步：你打标记
═══════════════════════════════════════════════════

  dataframe.loc[condition, 'exit_long'] = 1

═══════════════════════════════════════════════════
第 2 步：框架综合评估（freqtradebot.py:1396）
═══════════════════════════════════════════════════

  # handle_trade() → _check_and_execute_exit() → strategy.should_exit()
  # should_exit() 按以下优先级逐一检查：

  exits = self.strategy.should_exit(trade, rate, current_time, ...)
  # 内部优先级：
  #   1. stoploss       → 硬止损触发？→ execute_trade_exit(..., 'stop_loss')
  #   2. custom_stoploss → 动态止损触发？→ execute_trade_exit(...)
  #   3. trailing_stop   → 移动止损触发？→ execute_trade_exit(...)
  #   4. minimal_roi     → ROI 触发？→ execute_trade_exit(..., 'roi')
  #   5. custom_exit()   → 自定义退出？→ execute_trade_exit(..., 'custom_exit')
  #   6. exit_signal     → populate_exit_trend 的标记？
  #                        → execute_trade_exit(..., 'exit_signal')

  for should_exit in exits:
      if should_exit.exit_flag:
          self.execute_trade_exit(trade, exit_rate, should_exit, ...)

═══════════════════════════════════════════════════
第 3 步：下单平仓（freqtradebot.py:2083）
═══════════════════════════════════════════════════

  # execute_trade_exit() 内部：
  # ① 取消旧止损单
  self.exchange.cancel_stoploss_order(trade)

  # ② 准入确认
  if not strategy.confirm_trade_exit(pair, trade, order_type, amount, ...):
      return False

  # ③ 下单
  order = self.exchange.create_order(
      pair=pair,
      side="sell" if not is_short else "buy",  # ← 平多=卖出，平空=买入
      amount=amount,
      rate=exit_rate,
  )

  # ④ 通知 + 数据库更新
  rpc.send_msg({'type': 'exit', 'exit_reason': 'stop_loss', ...})
  trade.close()
```

### 做空（Short）同理

```python
# 你打标：
dataframe.loc[condition, 'enter_short'] = 1   # 声明：这里应该做空
dataframe.loc[condition, 'exit_short'] = 1    # 声明：这里应该平空

# 框架做的事：
# enter_short=1  → get_entry_signal() 返回 SignalDirection.SHORT
#                → execute_entry(side="sell", is_short=True)
#                → exchange.create_order(side="sell") ← 做空就是卖出

# exit_short=1   → should_exit() 检查
#                → execute_trade_exit(side="buy") ← 平空就是买入
```

做空需要 `can_short = True` + `trading_mode = "futures"`（现货不能做空）。

### 写策略时要注意什么

**1. 永远不要修改 OHLCV 数据**

```python
# ❌ 错误
dataframe['close'] = dataframe['close'] * 1.01

# ✅ 正确
dataframe['close_adjusted'] = dataframe['close'] * 1.01
```

**2. enter_long 和 exit_long 不能同时为 1**

同一根K线上，`enter_long=1` 且 `exit_long=1` 不会"先买后卖"——框架发现冲突后会忽略入场信号。如果你需要处理既有持仓又要新开仓的情况，用 `adjust_trade_position`。

**3. 入场信号只在"没有持仓的交易对"上生效**

框架对已有持仓的交易对不会再次调用 `create_trade()`（`enter_positions()` 会先排除已有持仓的交易对）。

**4. 离场信号的优先级低于止损和 ROI**

即使你设了 `exit_long=1`，如果止损先触发，离场信号会被跳过。这是设计上不可改变的（安全优先）。

**5. shift(1) 是防 lookahead 的关键**

```python
# ❌ 错误 — 会用当前K线数据做判断（lookahead）
dataframe.loc[dataframe['close'] > dataframe['sma'], 'enter_long'] = 1

# ✅ 正确 — 用上一根已闭台的K线数据
dataframe.loc[
    (dataframe['close'] > dataframe['sma']) &
    (dataframe['close'].shift(1) <= dataframe['sma'].shift(1)),  # 刚突破
    'enter_long'
] = 1
```

**6. 策略属性会被配置文件覆盖**

配置文件中的 `timeframe`、`stake_amount`、`max_open_trades` 等会覆盖策略中的同名设置。回测时日志里的 "Override strategy 'xxx' with value from the configuration" 就是这个意思。

**7. startup_candle_count 很重要**

如果你的 `populate_indicators` 用了 `EMA(200)`，必须设 `startup_candle_count = 200`，否则前 200 根K线的指标是 NaN，信号永远不会触发。

---

## 三、数据链路深度拆解

### 3.1 Exchange 层

#### OHLCV 获取与缓存

`Exchange._klines` 是内存缓存字典，键为 `(pair, timeframe, candle_type)`，值是该交易对的 DataFrame。

**`refresh_latest_ohlcv()` 核心流程：**

1. 为每个 `(pair, timeframe, candle_type)` 构建异步协程
2. 检查缓存是否仍有效（通过 `_now_is_time_to_refresh()` 判断新K线是否应该已生成）
3. 如果缓存有效 → 跳过
4. 如果缓存过期 → 调用 `_async_get_candle_history()` 拉取增量数据
5. 如果需要历史数据（启动时或数据不够） → 调用 `_async_get_historic_ohlcv()` 分页拉取
6. 并行执行所有协程（`asyncio.gather()`，最多同时 100 个）
7. 处理结果：转换 OHLCV → DataFrame，合并到已有缓存，截断到 `candle_limit`

**WebSocket 路径（当 `enable_ws=true` 时）：**
- 优选从 `self._ws_async` 的 `watch_ohlcv()` 缓存中直接取
- 如果没有 WS 或 WS 未就绪，回退到 REST API

#### 订单簿

`fetch_l2_order_book()` 使用**同步** API。被 `get_rate()` 调用以获取买卖价，用于定价。

### 3.2 DataProvider — 策略唯一数据入口

策略内部通过 `self.dp` 访问。关键方法：

| 方法 | 实盘数据来源 | 回测数据来源 |
|------|------------|------------|
| `get_analyzed_dataframe(pair, tf)` | `dp.__cached_pairs`（已分析的缓存） | 切片的已分析 DataFrame |
| `get_pair_dataframe(pair, tf)` | `Exchange._klines` 缓存 | 磁盘文件 |
| `ohlcv(pair, tf)` | `Exchange._klines` 缓存 | 空 DataFrame |
| `historic_ohlcv(pair, tf)` | — | 磁盘文件 |
| `ticker(pair)` | `Exchange.fetch_ticker()` | 同左
| `orderbook(pair, max)` | `Exchange.fetch_l2_order_book()` | 同左 |

**实盘中的刷新流程：**

```
FreqtradeBot.process()
  │
  ├─ dataprovider.refresh(whitelist_pairs, informative_pairs)
  │    │
  │    ├─ Exchange.refresh_latest_ohlcv(all_pairs)
  │    │     │
  │    │     ├─ 对每对 (pair, timeframe): 检查缓存是否过期
  │    │     ├─ 过期 → 异步 REST API 拉取增量K线
  │    │     └─ 存入 Exchange._klines 缓存
  │    │
  │    └─ (可选) refresh_latest_trades()
  │
  ├─ strategy.analyze(whitelist)
  │    │
  │    ├─ 对每个 pair:
  │    │     ├─ dp.ohlcv(pair, tf) → 读取 Exchange._klines
  │    │     ├─ _analyze_ticker_internal(dataframe, meta)
  │    │     │    ├─ @informative 装饰器方法（合并大周期数据）
  │    │     │    ├─ populate_indicators() → 你的指标
  │    │     │    ├─ populate_entry_trend() → 入场信号
  │    │     │    └─ populate_exit_trend() → 离场信号
  │    │     └─ dp._set_cached_df() → 存入 DataProvider 缓存
  │    │
  │    └─ dp._emit_df() → 发送给 RPC (FreqUI 图表)
  │
  └─ bot 使用分析后的 DataFrame 检查入场/离场信号
```

### 3.3 回测中的 lookahead bias 防止

回测的核心防 bias 机制：**信号偏移 1 根K线**。

在 `_get_ohlcv_as_lists()` 中，所有信号列（`enter_long`, `exit_long`, `enter_short`, `exit_short`）被**向后移动 1 行**。这意味着：

- 处理时间 T 的K线时，使用的信号来自 T-1 时刻闭台的K线
- 这和实盘行为一致：当前K线闭台后，你才知道上根K线的信号

此外：
- `DataProvider._set_dataframe_max_index()` 限制策略通过 `get_analyzed_dataframe()` 能"看到"的数据范围
- `DataProvider._set_dataframe_max_date()` 限制信息型交易对数据的时间范围

### 3.4 Exchange 双客户端与代理配置

```
ccxt_config → 传给同步 _api（下单、订单簿、余额）
ccxt_async_config → 传给异步 _api_async（K线、市场数据）
```

两个客户端需要**分别配置代理**（国内环境）：

```json
"ccxt_config": {
    "socksProxy": "socks5h://127.0.0.1:1080"
},
"ccxt_async_config": {
    "socksProxy": "socks5h://127.0.0.1:1080"
},
"enable_ws": false
```

注意：WebSocket 不走 SOCKS5 代理，国内环境必须关掉（`enable_ws: false`）。

### 3.5 需要安装的代理依赖

```bash
pip install aiohttp-socks pysocks
```

- `aiohttp-socks` → ccxt 异步客户端（aiohttp）走 SOCKS5
- `pysocks` → ccxt 同步客户端（requests）走 SOCKS5

---

## 四、回测机制详解

### 4.1 完整回测数据流

```
start_backtesting()
  │
  ├── 1. 数据加载  load_bt_data()
  │     ├── history.load_data()         → 从磁盘加载 .feather 文件
  │     ├── history.get_timerange()     → 确定公共日期范围
  │     ├── _load_bt_data_detail()      → 加载小周期/资金费率/标记价
  │     └── get_tick_size_over_time()   → 历史价格精度
  │
  ├── 2. 信号生成  backtest_one_strategy()
  │     ├── _set_strategy(strat)        → 注入 dp, wallets, ft_bot_start()
  │     ├── strategy.advise_all_indicators(data)  → ⚡ 一次性全量计算
  │     └── _get_ohlcv_as_lists(processed)
  │           ├── ft_advise_signals()   → 生成 entry/exit 信号
  │           ├── 移除 startup_candle_count 根K线
  │           ├── 🔑 信号偏移 1 行（防 lookahead）
  │           └── 转换为 Python list（性能优化）
  │
  ├── 3. 逐K线模拟  self.backtest()
  │     │
  │     ├── time_pair_generator()
  │     │     ├── 主时间框架循环
  │     │     ├── 细节时间框架循环（可选，用于K线内止损检查）
  │     │     └── 交易对循环（有持仓的交易对优先处理）
  │     │
  │     ├── backtest_loop(row, pair, time, trade_dir, can_enter)
  │     │     │
  │     │     ├── [A] manage_open_orders()
  │     │     │     ├── 超时过期订单检查
  │     │     │     └── adjust_order_price() 价格调整
  │     │     │
  │     │     ├── [B] _enter_trade()         ← 新入场
  │     │     │     ├── custom_entry_price()  价格调整
  │     │     │     ├── leverage()            杠杆确定
  │     │     │     ├── custom_stake_amount() 金额调整
  │     │     │     ├── confirm_trade_entry() 准入确认
  │     │     │     ├── 创建 LocalTrade + Order
  │     │     │     └── _try_close_open_order() 立即尝试成交
  │     │     │
  │     │     ├── [C] 填充未成交入场订单
  │     │     │
  │     │     ├── [D] _check_trade_exit()    ← 离场检查
  │     │     │     ├── _run_funding_fees()  期货资金费
  │     │     │     ├── _check_adjust_trade_for_candle() DCA
  │     │     │     ├── strategy.should_exit()
  │     │     │     │     ├── ft_stoploss_reached()  止损/清算
  │     │     │     │     ├── min_roi_reached()      ROI
  │     │     │     │     └── custom_exit()/exit_signal 信号
  │     │     │     └── _get_exit_for_signal()
  │     │     │           ├── _get_close_rate()      离场价
  │     │     │           ├── confirm_trade_exit()   准入
  │     │     │           └── _exit_trade()          创建离场Order
  │     │     │
  │     │     └── [E] _process_exit_order()  ← 成交离场订单
  │     │           ├── _try_close_open_order()  检查价格成交
  │     │           ├── trade.close()            关仓
  │     │           ├── wallets.update()         更新钱包
  │     │           └── run_protections()        保护检查
  │     │
  │     ├── handle_left_open()            → 回测结束强制平仓
  │     └── trade_list_to_dataframe()     → 转为 DataFrame
  │
  └── 4. 生成报告  generate_backtest_stats()
        ├── generate_pair_metrics()        → 每交易对指标
        ├── generate_tag_metrics()         → enter_tag + exit_reason
        ├── generate_trading_stats()       → 胜率、连赢、持仓时长
        ├── generate_daily_stats()         → 每日盈亏
        ├── calculate_max_drawdown()       → 最大回撤
        └── 计算 Sharpe, Sortino, Calmar, SQN

```

### 4.2 回测关键细节

**成交价确定：**
- 默认用当前K线的 **开盘价** 作为成交价（因为信号偏移了1根K线）
- 止损离场价：夹在 `[candle_low, candle_high]` 范围内
- 市价离场价：检查价格是否触及止损/ROI 水平

**手续费模型：**
- `fee_open` 和 `fee_close` 取交易所返回的 taker 和 maker 费率中的**最大值**（最差情况）
- 做多：`入场成本 = amount * rate * (1 + fee)`，`离场收益 = amount * rate * (1 - fee)`

**杠杆影响：**
- 投入金额：`stake_amount = amount * rate / leverage`
- 止损距离除以杠杆：1% stoploss at 10x → `0.001` (而非 `0.01`)
- 利润率乘以杠杆：`(close_value/open_value - 1) * leverage`

**细节时间框架：**
- 当配置了 `timeframe_detail`（如策略用 `5m`，细节用 `1m`）时
- 每根 5m K线被拆分为 5 根 1m 子K线
- 子K线继承父K线的 OHLCV + 信号
- 允许在 5m K线内部检查止损/ROI（更精准），但信号本身在主K线级别处理

### 4.3 回测中的 order_types 处理

- `stoploss_on_exchange` 在回测中被**强制设为 False**（回测假设"完美止损执行"）
- 止损单在回测中立即执行，不考虑交易所实际延迟
- 市价单和限价单有差异，但都是在下一根K线开盘价成交

---

## 五、实盘/模拟盘机制详解

### 5.1 主循环节流机制

Worker 的两个关键控制：

```python
# worker.py _throttle() 核心逻辑

sleep_duration = throttle_secs - time_passed  # 最少间隔 5 秒
if timeframe:
    next_tf = timeframe_to_next_date(timeframe)      # 下一根K线的时间
    next_tft = next_tf.timestamp() - time.time()     # 距下一根K线还有多久
    sleep_duration = min(sleep_duration, next_tft + 1)  # K线闭台后 1 秒
```

效果：
- 最快每 `process_throttle_secs`（默认 5 秒）迭代一次
- 下一根K线闭台后睡眠结束，精确定时在新K线生成后约 1 秒醒来
- 对 5m K线周期：bot 每 5 分钟醒来一次处理新数据

### 5.2 入场流程

```
create_trade(pair)
  │
  ├── dp.get_analyzed_dataframe(pair, tf)     → 拿已分析的 DataFrame
  ├── strategy.get_entry_signal(pair, tf, df)  → 读最后一行 enter_long/enter_short
  │     │
  │     ├── get_latest_candle() → 取最后一行
  │     ├── 读取 enter_long / enter_short 列
  │     ├── ignore_expired_candle() 检查
  │     └── 返回 (SignalDirection.LONG|SHORT, enter_tag)
  │
  └── execute_entry(pair, stake, enter_tag, is_short)
        │
        ├── get_valid_enter_price_and_stake()
        │     ├── exchange.get_rate(side="entry")     → 当前价
        │     ├── custom_entry_price()                → 策略可覆盖
        │     ├── leverage()                          → 期货杠杆
        │     ├── custom_stake_amount()               → 策略可覆盖金额
        │     └── wallets.validate_stake_amount()     → 余额检查
        │
        ├── confirm_trade_entry()                 → 准入确认
        │
        ├── exchange.create_order()               → 下单
        │     ├── [dry_run] create_dry_run_order() → 模拟：创建本地Order并用UUID成交
        │     └── [live] ccxt.create_order()       → 真实订单
        │
        ├── rpc._notify_enter()                   → Telegram/WebUI 通知
        └── update_trade_state()                  → 立即检查是否成交
```

### 5.3 离场流程

离场由 `handle_trade()` → `should_exit()` → `execute_trade_exit()` 处理：

```
handle_trade(trade)
  │
  ├── dp.get_analyzed_dataframe(trade.pair, tf)
  ├── strategy.get_exit_signal()       → 读 exit_long/exit_short 列
  │
  └── _check_and_execute_exit(trade, rate, enter, exit_, tag)
        │
        ├── strategy.should_exit()     → 综合评估所有离场条件
        │     │                          (按优先级: stoploss → custom_stoploss
        │     │                           → trailing → ROI → custom_exit → exit_signal)
        │     └── 返回 [ExitCheckTuple, ...]
        │
        └── execute_trade_exit()
              ├── cancel_stoploss_on_exchange()  → 取消交易所止损单
              ├── strategy.confirm_trade_exit()  → 准入确认
              ├── exchange.create_order()        → 下离场单
              ├── rpc._notify_exit()             → 通知
              └── handle_protections()           → 触发保护（如 StoplossGuard）
```

### 5.4 dry_run vs live — 代码路径对比

| 方面 | dry_run（模拟） | live（实盘） |
|------|----------------|-------------|
| 数据库 | `tradesv3.dryrun.sqlite` | `tradesv3.sqlite` |
| 下单 | `create_dry_run_order()` → UUID 订单，本地检查成交 | `ccxt.create_order()` → 真实交易所订单 |
| 订单取消 | 更新本地状态为 `"canceled"` | `ccxt.cancel_order()` → 交易所 API |
| 钱包 | 从数据库计算：`start_cap + 盈亏 - 占用` | `exchange.get_balances()` 真实查询 |
| 启动恢复 | 跳过 `startup_update_open_orders()` | 从交易所同步所有未结订单状态 |
| 手续费 | 按费率 × 交易量计算 | 从交易所返回的订单详情提取 |

**核心结论：** 除了 Exchange 层的订单创建/查询/取消和钱包查询外，**所有其他代码完全相同**。策略代码不需要任何改动。

### 5.5 异常处理与容错

freqtrade 有精细的异常层次结构：

```
FreqtradeException
  ├── OperationalException    → 严重错误，停止 bot
  ├── DependencyException     → 暂时跳过（单次操作失败）
  │     ├── PricingError       → 无法定价，跳过此交易
  │     └── ExchangeError
  │           ├── InvalidOrderException
  │           │     ├── RetryableOrderError  → 指数退避重试
  │           │     └── InsufficientFundsError → 余额不足
  │           └── TemporaryError            → 网络问题，稍后重试
  │                 └── DDosProtection       → 限频，休眠后重试
  └── StrategyError           → 策略代码错误
```

**Worker 级别：**
- `TemporaryError` → 休眠 30 秒后重试（不停止 bot）
- `OperationalException` → 切换到 STOPPED 状态

**FreqtradeBot 级别：**
- 单笔交易失败不级联 → 日志警告 + 跳过该交易对
- `InsufficientFundsError` → 尝试通过重新获取订单来恢复
- 止损单的 `InvalidOrderException` → **紧急退出**（市价单强制平仓）

---

## 六、回测 vs 实盘对比

### 6.1 完全相同的部分

```python
# ✅ 回测和实盘一模一样
populate_indicators()
populate_entry_trend()
populate_exit_trend()
custom_exit()
custom_stoploss()
adjust_trade_position()
custom_entry_price()
custom_exit_price()
custom_stake_amount()
confirm_trade_entry()
confirm_trade_exit()
leverage()
check_entry_timeout()
check_exit_timeout()
order_filled()
```

### 6.2 只在实盘中调用的

```python
# ❌ 回测不调
bot_start()
bot_loop_start()
adjust_entry_price()   # 只在实盘中管理未成交订单
adjust_exit_price()
```

### 6.3 关键差异总结

| 维度 | 回测 | 实盘 |
|------|------|------|
| 数据来源 | 磁盘 .feather 文件 | 交易所 API 实时拉取 |
| 指标计算 | 一次性全量 | 增量（仅新K线） |
| 信号处理 | 偏移 1 行防 lookahead | 自然无 lookahead |
| 订单成交 | 下根K线开盘价立即成交 | 真实市场撮合 |
| 滑点 | 无 | 模拟盘有（默认 5%） |
| 部分成交 | 无 | 可能发生（IOC/FOK） |
| 网络延迟 | 无 | 有 |
| 限频 | 无 | 交易所限频 |
| 确认回调 | 调用 confirm_trade_entry/exit | 同样调用 |

### 6.4 回测局限性

| 局限 | 影响 |
|------|------|
| 无真实滑点 | 限价单可能看起来比实盘成交更好 |
| 无部分成交 | 大单总是全额成交 |
| 无交易延迟 | 不考虑下单到成交的时间差 |
| 止损立即执行 | 实盘中止损可能有滑点 |
| 流动性假设 | 假设总有对手盘 |

---

## 七、从零开始写策略

### 7.1 最小策略模板

```python
# user_data/strategies/MyStrategy.py
from freqtrade.strategy import IStrategy
from pandas import DataFrame
import talib.abstract as ta


class MyStrategy(IStrategy):
    timeframe = '5m'
    can_short = False
    minimal_roi = {"60": 0.01, "30": 0.02, "0": 0.05}
    stoploss = -0.10

    def populate_indicators(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)
        dataframe['sma_20'] = ta.SMA(dataframe, timeperiod=20)
        dataframe['sma_50'] = ta.SMA(dataframe, timeperiod=50)
        return dataframe

    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        dataframe['enter_long'] = 0
        dataframe.loc[
            (dataframe['rsi'] < 30) &
            (dataframe['sma_20'] > dataframe['sma_50']) &
            (dataframe['volume'] > 0),
            ['enter_long', 'enter_tag']
        ] = (1, 'rsi_oversold_uptrend')
        return dataframe

    def populate_exit_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        dataframe['exit_long'] = 0
        dataframe.loc[dataframe['rsi'] > 70, 'exit_long'] = 1
        return dataframe
```

### 7.2 进阶：加入 custom_exit 和 custom_stoploss

```python
def custom_exit(self, pair, trade, current_time, current_rate,
                current_profit, **kwargs):
    """到达目标盈利就退出"""
    if current_profit > 0.03:
        return 'target_profit'
    # 持仓超过 12 小时且不盈利也退出
    if (current_time - trade.open_date_utc).seconds > 43200 and current_profit < 0:
        return 'timeout_exit'
    return None

def custom_stoploss(self, pair, trade, current_time, current_rate,
                    current_profit, after_fill, **kwargs):
    """动态止损：盈利越多止损越紧"""
    if current_profit > 0.05:
        return -0.01   # 盈利超 5%，止损收紧到 -1%
    if current_profit > 0.02:
        return -0.03   # 盈利超 2%，止损 -3%
    return -0.10       # 默认 -10%
```

### 7.3 开发工作流

```
  1. 写策略代码
      user_data/strategies/MyStrategy.py
           │
  2. 回测验证
      freqtrade backtesting -s MyStrategy
           │
  3. 画图检查信号
      freqtrade plot-dataframe -s MyStrategy -p BTC/USDT
      freqtrade plot-profit -s MyStrategy
           │
  4. 模拟盘验证（数天~数周）
      freqtrade trade -s MyStrategy --dry_run
           │
  5. 实盘（小资金起步）
      dry_run=false + 小 stake_amount
```

### 7.4 调试技巧

**在策略中输出调试日志：**
```python
import logging
logger = logging.getLogger(__name__)

def populate_entry_trend(self, dataframe, metadata):
    signals = len(dataframe[dataframe['rsi'] < 30])
    logger.info(f"{metadata['pair']}: {signals} potential entry signals")
    ...
```

**在回调中通过 dp 获取数据：**
```python
def custom_exit(self, pair, trade, current_time, current_rate, current_profit, **kwargs):
    dataframe, _ = self.dp.get_analyzed_dataframe(pair, self.timeframe)
    last = dataframe.iloc[-1]
    logger.info(f"{pair}: rsi={last['rsi']:.2f}, profit={current_profit:.4f}")
    ...
```

---

## 关键文件索引

| 文件 | 作用 |
|------|------|
| `freqtrade/strategy/interface.py` | **IStrategy 基类** — 所有策略回调的定义和管道 |
| `freqtrade/strategy/hyper.py` | **超参混入** — IntParameter / RealParameter 等 |
| `freqtrade/strategy/parameters.py` | 参数类定义 |
| `freqtrade/strategy/informative_decorator.py` | **@informative 装饰器** |
| `freqtrade/strategy/strategy_wrapper.py` | **安全包装器** — 策略调用的异常隔离和 Trade 深拷贝 |
| `freqtrade/optimize/backtesting.py` | **回测引擎** — 完整的回测模拟逻辑 |
| `freqtrade/freqtradebot.py` | **实盘/模拟盘交易引擎** — process() / create_trade() 等 |
| `freqtrade/worker.py` | **主循环** — 状态机和节流控制 |
| `freqtrade/exchange/exchange.py` | **交易所抽象层** — ccxt 双客户端、OHLCV 缓存、订单管理 |
| `freqtrade/data/dataprovider.py` | **数据提供者** — 策略访问数据的唯一入口 |
| `freqtrade/rpc/rpc_manager.py` | **消息分发** — Telegram、API Server 的多路复用 |
| `freqtrade/rpc/telegram.py` | **Telegram 机器人** |
| `freqtrade/wallets.py` | **钱包管理** — 实盘查交易所，模拟查数据库 |

---

## 总结

| 问题 | 答案 |
|------|------|
| 回测和实盘用同一套代码吗？ | **是**，除了 Exchange 层下单/查询/取消不同，其他全部相同 |
| 策略能跑什么模式？ | 回测、超参优化、模拟盘、实盘 — 同一份策略代码全部兼容 |
| 策略 API 有多灵活？ | 30+ 个可覆盖的回调，覆盖价格、金额、准入、止损、DCA 等所有环节 |
| 什么是防 lookahead bias？ | 回测中信号偏移 1 根K线 + DataProvide 切片限制 |
| 策略文件放哪里？ | `user_data/strategies/` 下的 `.py` 文件，类名=文件名=策略名 |
