# Freqtrade 用户指南

> 本文档帮助你快速了解 freqtrade 项目的目录结构、配置文件、策略开发流程和日常开发工作流。
>
> 环境：Python 3.13 / conda 环境 `freqtrade`

---

## 一、项目整体结构

克隆下来的项目长这样：

```
freqtrade/                          ← 项目根目录
├── freqtrade/                      ← 📦 核心源码（内部引擎，一般不需要改）
├── user_data/                      ← 👈 你的主工作区（策略 + 配置 + 数据）
├── tests/                          ← 🧪 单元测试和集成测试
├── config_examples/                ← 📋 配置文件模板（参考用）
├── docs/                           ← 📖 在线文档 Markdown 源码
├── scripts/                        ← 🔧 辅助脚本（REST/WebSocket 测试客户端）
├── build_helpers/                  ← 🏗️ 打包/发布工具
├── ft_client/                      ← 🌐 REST API 客户端库
├── docker/                         ← 🐳 Docker 镜像构建文件
├── freqtrade/templates/            ← 📝 策略模板和配置模板
├── pyproject.toml                  ← Python 项目元数据和依赖声明
├── requirements*.txt               ← pip 依赖文件（按模块拆分）
├── setup.sh / setup.ps1            ← 一键安装脚本（Linux/macOS / Windows）
├── docker-compose.yml              ← Docker Compose 编排文件
├── mkdocs.yml                      ← 文档站点配置
└── 用户指南.md                     ← 📖 你在看的这个文件
```

---

## 二、你的主工作区：`user_data/`

`user_data/` 是唯一一个你日常开发需要关心的目录。所有策略、配置、数据都在这里：

```
user_data/
├── strategies/                     ← 🧠 你的交易策略 Python 文件
├── config.json                     ← ⚙️ 主配置文件（需要自己创建）
├── data/                           ← 💾 下载的 OHLCV K线数据
│   └── binance/                    ← 按交易所分目录
├── logs/                           ← 📜 运行日志
│   └── freqtrade.log
├── backtest_results/               ← 📊 回测结果（CSV/JSON）
├── freqaimodels/                   ← 🤖 FreqAI 训练好的机器学习模型
├── hyperopts/                      ← 🔬 超参优化结果
└── notebooks/                      ← 📓 Jupyter Notebook 分析笔记
```

> **你只会在这里新建和编辑文件，核心源码 `freqtrade/` 不要修改。**

---

## 三、配置文件详解

### 3.1 配置模板参考

`config_examples/` 目录下有完整的配置模板：

| 文件 | 说明 |
|------|------|
| `config_full.example.json` | **最完整**的配置，所有选项含注释，建议通读 |
| `config_binance.example.json` | Binance 交易所专用 |
| `config_kraken.example.json` | Kraken 交易所专用 |
| `config_freqai.example.json` | FreqAI 机器学习功能配置 |

### 3.2 最小可用配置

将以下内容保存为 `user_data/config.json` 即可启动模拟交易（dry-run）：

```json
{
    "max_open_trades": 3,
    "stake_currency": "USDT",
    "stake_amount": 100,
    "tradable_balance_ratio": 0.99,
    "dry_run": true,

    "exchange": {
        "name": "binance",
        "key": "",
        "secret": ""
    },

    "pairlists": [
        {"method": "StaticPairList"}
    ],

    "entry_pricing": {
        "price_side": "same",
        "use_order_book": false
    },
    "exit_pricing": {
        "price_side": "same",
        "use_order_book": false
    }
}
```

### 3.3 关键配置项说明

```jsonc
{
    // ── 资金管理 ──
    "max_open_trades": 3,              // 最大同时持仓数量
    "stake_currency": "USDT",          // 报价货币
    "stake_amount": 100,               // 每笔交易投入金额（USDT）
    "tradable_balance_ratio": 0.99,    // 可用资金比例（0.99 = 留 1% 备用）

    // ── 运行模式 ──
    "dry_run": true,                   // true=模拟交易, false=实盘

    // ── 交易所 ──
    "exchange": {
        "name": "binance",             // 交易所名称
        "key": "YOUR_API_KEY",         // API Key（dry_run 可留空）
        "secret": "YOUR_SECRET"        // API Secret（dry_run 可留空）
    },

    // ── 交易对选择 ──
    "pairlists": [
        {"method": "StaticPairList"}   // 静态交易对列表
    ],
    "exchange": {
        "pair_whitelist": [            // 要交易的币对
            "BTC/USDT",
            "ETH/USDT",
            "SOL/USDT"
        ]
    },

    // ── 买卖定价 ──
    "entry_pricing": {
        "price_side": "same",
        "use_order_book": false
    },
    "exit_pricing": {
        "price_side": "same",
        "use_order_book": false
    },

    // ── Telegram 通知（可选）──
    "telegram": {
        "enabled": false,
        "token": "YOUR_BOT_TOKEN",
        "chat_id": "YOUR_CHAT_ID"
    },

    // ── API 服务器（可选）──
    "api_server": {
        "enabled": true,
        "listen_ip_address": "127.0.0.1",
        "listen_port": 8080,
        "username": "freqtrader",
        "password": "password"
    },

    // ── FreqAI（可选）──
    "freqai": {
        "enabled": false
    }
}
```

### 3.4 创建配置文件的方法

**方法一：命令行向导（推荐入门）**

```bash
conda activate freqtrade
freqtrade new-config --config user_data/config.json
```

交互式问答，填入交易所、资金、交易对等信息，自动生成配置文件。

**方法二：复制示例再修改**

```bash
cp config_examples/config_full.example.json user_data/config.json
# 然后用编辑器打开修改
```

---

## 四、策略开发

### 4.1 策略放在哪里？

```
user_data/strategies/
├── MyFirstStrategy.py       ← 你自己的策略
├── MACDStrategy.py          ← 另一个策略
└── ...
```

> 一个文件一个策略，文件名即策略名，类名和文件名保持一致。

### 4.2 策略骨架

最小策略模板：

```python
# user_data/strategies/MyFirstStrategy.py

from freqtrade.strategy import IStrategy
from pandas import DataFrame
import talib.abstract as ta


class MyFirstStrategy(IStrategy):

    # ── 时间框架 ──
    timeframe = '5m'

    # ── 资金管理 ──
    minimal_roi = {
        "60": 0.01,    # 60 分钟后，盈利 1% 就止盈
        "30": 0.02,    # 30 分钟
        "0": 0.04      # 立即止盈 4%
    }
    stoploss = -0.10   # 止损 -10%

    # ── 买入信号 ──
    def populate_entry_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        在这里写买入逻辑。
        dataframe 里已经有 OHLCV 数据和你计算好的指标。
        设置 dataframe['enter_long'] = 1 表示买入。
        """
        dataframe['enter_long'] = 0

        # 示例：RSI < 30 时买入
        dataframe.loc[
            (dataframe['rsi'] < 30),
            'enter_long'
        ] = 1

        return dataframe

    # ── 卖出信号 ──
    def populate_exit_trend(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        在这里写卖出逻辑。
        设置 dataframe['exit_long'] = 1 表示卖出。
        """
        dataframe['exit_long'] = 0

        # 示例：RSI > 70 时卖出
        dataframe.loc[
            (dataframe['rsi'] > 70),
            'exit_long'
        ] = 1

        return dataframe

    # ── 指标计算 ──
    def populate_indicators(self, dataframe: DataFrame, metadata: dict) -> DataFrame:
        """
        在这里计算技术指标。
        添加的列可以在 populate_entry_trend / populate_exit_trend 里使用。
        """
        # RSI
        dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)

        # MACD
        macd = ta.MACD(dataframe)
        dataframe['macd'] = macd['macd']
        dataframe['macdsignal'] = macd['macdsignal']

        # 布林带
        bollinger = ta.BBANDS(dataframe, timeperiod=20)
        dataframe['bb_upperband'] = bollinger['upperband']
        dataframe['bb_middleband'] = bollinger['middleband']
        dataframe['bb_lowerband'] = bollinger['lowerband']

        return dataframe
```

### 4.3 创建策略的命令

```bash
# 创建基础模板策略
freqtrade new-strategy -s MyStrategy

# 创建高级模板（含更多指标示例）
freqtrade new-strategy -s MyStrategy --template advanced

# 创建 FreqAI 机器学习策略
freqtrade new-strategy -s MyFreqAIStrategy --template freqai
```

### 4.4 可用的技术指标

策略中可以直接使用：

```python
import talib.abstract as ta
import freqtrade.vendor.qtpylib.indicators as qtpylib

# TA-Lib 指标（170+ 种）
dataframe['rsi'] = ta.RSI(dataframe, timeperiod=14)
dataframe['sma'] = ta.SMA(dataframe, timeperiod=50)
dataframe['ema'] = ta.EMA(dataframe, timeperiod=200)
dataframe['bb_upper'], dataframe['bb_middle'], dataframe['bb_lower'] = ta.BBANDS(dataframe)
dataframe['macd'] = ta.MACD(dataframe)['macd']
dataframe['atr'] = ta.ATR(dataframe, timeperiod=14)
dataframe['adx'] = ta.ADX(dataframe, timeperiod=14)
dataframe['stoch_k'], dataframe['stoch_d'] = ta.STOCH(dataframe)

# 自定义 Pandas TA 指标
dataframe['zscore'] = dataframe['close'].rolling(20).apply(lambda x: (x[-1] - x.mean()) / x.std())
dataframe['volume_sma'] = dataframe['volume'].rolling(20).mean()
```

### 4.5 Minimal ROI（止盈曲线）

```python
minimal_roi = {
    "720": 0,       # 720 分钟后，不设止盈门槛
    "120": 0.01,    # 120 分钟，盈利 1% 止盈
    "60": 0.02,     # 60 分钟，盈利 2% 止盈
    "0": 0.05       # 买入后立即，盈利 5% 止盈
}
```

含义：买入越久，止盈门槛越低，鼓励及时获利了结。

---

## 五、日常命令速查

### 激活环境

```bash
conda activate freqtrade
```

### 数据下载

```bash
# 下载币安 BTC/USDT 的 5 分钟数据
freqtrade download-data -t 5m -p BTC/USDT

# 下载多个币对（-t 后可接多个时间周期）
freqtrade download-data -t 5m 15m 1h -p BTC/USDT ETH/USDT SOL/USDT

# 指定时间范围
freqtrade download-data -t 5m -p BTC/USDT --timerange 20250101-20250801
```

### 回测

```bash
# 用你的策略跑回测
freqtrade backtesting -s MyFirstStrategy

# 指定时间范围
freqtrade backtesting -s MyFirstStrategy --timerange 20250101-20250801

# 输出更详细的结果
freqtrade backtesting -s MyFirstStrategy --timerange 20250101-20250801 --export signals
```

### 画图分析

```bash
# 画出策略在某个币对上的买卖信号
freqtrade plot-dataframe -s MyFirstStrategy -p BTC/USDT

# 画出收益曲线
freqtrade plot-profit -s MyFirstStrategy -p BTC/USDT
```

### 模拟交易（Dry-Run）

```bash
# 启动模拟交易（需要先配置好 config.json）
freqtrade trade -c user_data/config.json -s MyFirstStrategy

# 启动时开启 Web 界面（访问 http://127.0.0.1:8080）
freqtrade trade -c user_data/config.json -s MyFirstStrategy --config config_examples/config_full.example.json
```

### 运行测试

```bash
# 运行全部测试
pytest tests/

# 运行单个测试文件
pytest tests/strategy/test_strategy.py

# 只跑你的策略的回测
pytest tests/optimize/test_backtesting.py -k test_backtest

# 代码检查
ruff check freqtrade/

# 类型检查
mypy freqtrade/
```

### 其他

```bash
# 列出所有可用交易所
freqtrade list-exchanges

# 列出交易所支持的币对
freqtrade list-pairs -e binance

# 列出所有已下载数据的时间框架
freqtrade list-data

# 列出 user_data/strategies/ 下所有策略
freqtrade list-strategies
```

---

## 六、典型开发流程

### 从头开始的完整流程

```bash
# 1. 激活环境
conda activate freqtrade

# 2. 初始化用户目录
freqtrade create-userdir --userdir user_data/

# 3. 创建配置文件
freqtrade new-config --config user_data/config.json

# 4. 下载历史数据
freqtrade download-data -t 5m -p BTC/USDT ETH/USDT --timerange 20250101-20250801

# 5. 生成策略模板
freqtrade new-strategy -s MyStrategy --template advanced

# 6. 编辑策略文件
# 用 VS Code / PyCharm 打开 user_data/strategies/MyStrategy.py
# 实现 populate_indicators、populate_entry_trend、populate_exit_trend

# 7. 回测验证
freqtrade backtesting -s MyStrategy --timerange 20250101-20250601

# 8. 画图看信号
freqtrade plot-dataframe -s MyStrategy -p BTC/USDT

# 9. 迭代优化 —— 修改策略 → 重新回测 → 看结果 → 再修改

# 10. 模拟交易验证
freqtrade trade -c user_data/config.json -s MyStrategy
```

### 日常迭代流程（已有配置和数据）

```bash
# 1. 改策略代码
# 2. 跑回测看效果
freqtrade backtesting -s MyStrategy --timerange 20250601-20250801
# 3. 看不满意 → 回到第 1 步
# 4. 满意 → 切到模拟交易
freqtrade trade -c user_data/config.json -s MyStrategy
```

---

## 七、目录 vs 只读等级

| 目录 | 你平时要改吗？ | 说明 |
|------|:---:|------|
| `user_data/strategies/` | ✅ | **你写策略的地方** |
| `user_data/config.json` | ✅ | **你的配置文件** |
| `user_data/data/` | ⬇️ | 命令下载数据到这里 |
| `user_data/logs/` | 👀 | 看日志用 |
| `freqtrade/` | ❌ | 核心源码，不要改 |
| `freqtrade/templates/` | 👀 | 参考模板，不要改 |
| `tests/` | ❌ | 测试代码 |
| `config_examples/` | 👀 | 配置参考，不要改 |
| `docs/` | 👀 | 文档源码 |
| `scripts/` | 👀 | 辅助脚本 |

---

## 八、常见问题

**Q: dry_run 模式下需要 API Key 吗？**

不需要。`"key": ""` 和 `"secret": ""` 留空即可，模拟交易使用交易所的公开行情。

**Q: 回测需要联网吗？**

需要先下载数据（联网），之后回测是离线的。

**Q: 配置文件里币对写了，但回测提示找不到数据？**

先确认数据已下载：`freqtrade list-data`，然后确认配置里 `pair_whitelist` 匹配。

**Q: 策略里的 `metadata` 参数是什么？**

一个字典，包含当前币对信息，如 `metadata['pair']` → `'BTC/USDT'`。

**Q: 策略文件名和类名有什么关系？**

保持一致。文件名 `MyStrategy.py` → 类名 `MyStrategy`，freqtrade 通过文件名查找策略。
