# Freqtrade 数据库说明文档

> 本文档详细说明 freqtrade 的数据库架构、表结构、配置方式、迁移机制，以及从 SQLite 切换到 MySQL/PostgreSQL 的操作指南。

---

## 目录

1. [概述](#一概述)
2. [数据库自动创建机制](#二数据库自动创建机制)
3. [六张表完整结构](#三六张表完整结构)
4. [Dry-run 与 Live 数据库隔离](#四dry-run-与-live-数据库隔离)
5. [回测模式：无数据库运行](#五回测模式无数据库运行)
6. [切换到 MySQL / PostgreSQL](#六切换到-mysql--postgresql)
7. [数据库迁移](#七数据库迁移)
8. [常见操作](#八常见操作)

---

## 一、概述

### 技术栈

| 组件 | 选型 |
|------|------|
| ORM 框架 | SQLAlchemy 2.x（DeclarativeBase） |
| 默认数据库 | SQLite（文件型，零配置） |
| 支持的后端 | SQLite / PostgreSQL / MySQL |
| 迁移方式 | 自研（非 Alembic），自动检测并迁移 |
| 表数量 | 6 张 |

### 架构图

```
FreqtradeBot
    │
    ├── Trade (SQLAlchemy ORM)  ←→  trades 表
    │     └── Order (一对多)    ←→  orders 表
    │     └── _CustomData       ←→  trade_custom_data 表
    │
    ├── PairLock               ←→  pairlocks 表
    ├── WalletHistory          ←→  wallet_history 表
    ├── KeyValueStore          ←→  KeyValueStore 表
    │
    └── init_db(db_url)
          ├── create_engine(db_url)        # SQLAlchemy 引擎
          ├── ModelBase.metadata.create_all()  # 自动建表
          └── check_migrate()              # 运行迁移
```

### 关键源码文件

| 文件 | 作用 |
|------|------|
| `freqtrade/persistence/base.py` | SQLAlchemy 声明式基类 `ModelBase` |
| `freqtrade/persistence/models.py` | `init_db()` — 引擎创建、会话工厂、建表入口 |
| `freqtrade/persistence/trade_model.py` | `Trade` 和 `Order` ORM 模型（核心） |
| `freqtrade/persistence/custom_data.py` | `_CustomData` — 策略自定义键值数据 |
| `freqtrade/persistence/key_value_store.py` | `_KeyValueStoreModel` — 全局键值存储 |
| `freqtrade/persistence/pairlock.py` | `PairLock` — 交易对锁定 |
| `freqtrade/persistence/wallet_history.py` | `WalletHistory` — 每日钱包快照 |
| `freqtrade/persistence/migrations.py` | 数据库迁移逻辑 |

---

## 二、数据库自动创建机制

### 核心代码

`freqtrade/persistence/models.py` 中的 `init_db()`：

```python
def init_db(db_url: str) -> None:
    # 1. 创建 SQLAlchemy 引擎
    engine = create_engine(db_url, future=True, **kwargs)

    # 2. 创建线程安全的 scoped_session
    Trade.session = scoped_session(sessionmaker(bind=engine, autoflush=False))

    # 3. 所有模型共享同一个 session
    Order.session = Trade.session
    PairLock.session = Trade.session
    KeyValueStore.session = Trade.session
    WalletHistory.session = Trade.session

    # 4. 自动建表！
    ModelBase.metadata.create_all(engine)

    # 5. 运行迁移（如果需要）
    check_migrate(engine)
```

### 触发时机

每次启动 `freqtrade trade` 时自动调用 `init_db()`。如果数据库文件不存在，SQLite 会自动创建 `.sqlite` 文件，然后 `create_all()` 建表。

**不需要手动运行任何建表脚本。**

### 数据库 URL 决定逻辑

```
配置文件中的 db_url
    │
    ├── dry_run=true 且 db_url 未设置
    │     → sqlite:///tradesv3.dryrun.sqlite
    │
    ├── dry_run=false 且 db_url 未设置
    │     → sqlite:///tradesv3.sqlite
    │
    └── 用户显式设置了 db_url
          → 使用用户指定的值
```

也可以通过命令行覆盖：

```bash
freqtrade trade -c config.json --db-url mysql+pymysql://user:pass@localhost/freqtrade
```

---

## 三、六张表完整结构

### 3.1 trades — 交易记录（核心表）

54 列，记录每笔交易的完整生命周期。

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键，即 trade_id |
| `exchange` | String(25) | 交易所名称，如 `binance` |
| `pair` | String(25) | 交易对，如 `BTC/USDT` |
| `base_currency` | String(25) | 基础货币，如 `BTC` |
| `stake_currency` | String(25) | 计价货币，如 `USDT` |
| `is_open` | Boolean | 是否未平仓（True=持仓中） |
| `open_rate` | Float | 开仓均价 |
| `close_rate` | Float | 平仓均价 |
| `stake_amount` | Float | 投入金额（计价货币） |
| `amount` | Float | 持仓数量（基础货币） |
| `open_date` | DateTime | 开仓时间 |
| `close_date` | DateTime | 平仓时间 |
| `fee_open` / `fee_close` | Float | 开/平仓手续费率 |
| `fee_open_cost` / `fee_close_cost` | Float | 开/平仓手续费金额 |
| `fee_open_currency` / `fee_close_currency` | String(25) | 手续费币种 |
| `close_profit` | Float | 平仓盈亏比率 |
| `close_profit_abs` | Float | 平仓盈亏绝对值 |
| `realized_profit` | Float | 已实现盈亏（部分平仓场景） |
| `stop_loss` | Float | 当前止损价 |
| `stop_loss_pct` | Float | 当前止损比率 |
| `initial_stop_loss` | Float | 初始止损价 |
| `exit_reason` | String(255) | 退出原因：`stop_loss`、`roi`、`exit_signal`、`custom_exit` 等 |
| `enter_tag` | String(255) | 入场标签（策略中设置的信号名称） |
| `strategy` | String(100) | 使用的策略名称 |
| `timeframe` | Integer | K线周期（分钟） |
| `trading_mode` | Enum | `spot` / `futures` / `margin` |
| `leverage` | Float | 杠杆倍数（期货，默认 1.0） |
| `is_short` | Boolean | 是否做空 |
| `liquidation_price` | Float | 强平价格（期货） |
| `funding_fees` | Float | 累计资金费率 |
| `is_stop_loss_trailing` | Boolean | 是否启用移动止损 |
| `max_rate` / `min_rate` | Float | 持仓期间最高/最低价 |
| `contract_size` | Float | 合约面值 |
| `precision_mode` | Integer | 精度模式 |
| `amount_precision` / `price_precision` | Float | 数量/价格精度 |

**索引：** `pair`、`is_open` 有索引（高频查询列）。

**关系：**
- `orders` → 一对多，一个 Trade 可以有多个 Order（加仓/减仓场景）
- `custom_data` → 一对多，Trade 的自定义元数据

### 3.2 orders — 订单记录

26 列，记录交易所的每一笔订单。

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键 |
| `ft_trade_id` | Integer (FK→trades.id) | 所属交易 ID |
| `ft_order_side` | String(25) | 订单方向：`buy` / `sell` / `stoploss` |
| `ft_pair` | String(25) | 交易对 |
| `ft_is_open` | Boolean | 订单是否仍未成交 |
| `ft_amount` | Float | 订单数量 |
| `ft_price` | Float | 订单价格 |
| `ft_cancel_reason` | String | 取消原因 |
| `order_id` | String(255) | 交易所返回的订单 ID |
| `status` | String(255) | 订单状态：`open` / `closed` / `canceled` / `expired` |
| `order_type` | String(50) | 订单类型：`limit` / `market` |
| `side` | String(25) | `buy` / `sell` |
| `price` | Float | 委托价格 |
| `average` | Float | 成交均价 |
| `amount` | Float | 委托数量 |
| `filled` | Float | 已成交数量 |
| `remaining` | Float | 未成交数量 |
| `cost` | Float | 成交金额（含手续费） |
| `order_date` | DateTime | 下单时间 |
| `order_filled_date` | DateTime | 成交时间 |
| `funding_fee` | Float | 资金费率 |
| `ft_fee_base` | Float | 手续费（基础货币） |
| `ft_order_tag` | String | 订单标签 |

**唯一约束：** `(ft_pair, order_id)` — 同一交易对的订单 ID 唯一。

### 3.3 pairlocks — 交易对锁定

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键 |
| `pair` | String(25) | 锁定的交易对，`*` 表示全局锁 |
| `side` | String(25) | 锁定的方向：`long` / `short` / `*` |
| `reason` | String(255) | 锁定原因 |
| `lock_time` | DateTime | 锁定开始时间 |
| `lock_end_time` | DateTime | 锁定结束时间 |
| `active` | Boolean | 是否仍生效 |

**用途：** 保护机制（StoplossGuard 等）触发后，自动锁定交易对一段时间，防止立即重新入场。

### 3.4 wallet_history — 钱包余额历史

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键 |
| `timestamp` | DateTime | 快照时间 |
| `currency` | String(25) | 币种 |
| `balance` | Float | 余额 |
| `quote_currency` | String(25) | 计价货币 |
| `rate` | Float | 汇率 |
| `total_quote` | Float | 折合计价货币总额 |
| `total_position_value` | Float | 持仓总价值 |
| `collateral` | Float | 保证金 |
| `leverage` | Float | 杠杆 |
| `bot_managed` | Boolean | 是否机器人管理 |

**用途：** 每日自动快照，用于绘制净值曲线、计算 Sharpe/Sortino 等指标。

### 3.5 KeyValueStore — 全局键值存储

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键 |
| `key` | String(50) | 键名（唯一） |
| `value_type` | String(20) | 值类型：`str` / `datetime` / `float` / `int` |
| `string_value` | String(255) | 字符串值 |
| `datetime_value` | DateTime | 日期时间值 |
| `float_value` | Float | 浮点值 |
| `int_value` | Integer | 整数值 |

**用途：** 存储 bot 启动时间、迁移状态标记等全局配置。

### 3.6 trade_custom_data — 策略自定义数据

| 列名 | 类型 | 说明 |
|------|------|------|
| `id` | Integer (PK) | 自增主键 |
| `ft_trade_id` | Integer (FK→trades.id) | 所属交易 ID |
| `cd_key` | String(255) | 键 |
| `cd_type` | String(25) | 类型 |
| `cd_value` | Text | JSON 序列化的值 |
| `created_at` | DateTime | 创建时间 |
| `updated_at` | DateTime | 更新时间 |

**用途：** 策略可以在回调中为每笔交易存储自定义数据（入场波动率、AI 预测分数等）。

---

## 四、Dry-run 与 Live 数据库隔离

### 默认行为

| 模式 | 默认数据库文件 | 说明 |
|------|--------------|------|
| dry_run=true | `tradesv3.dryrun.sqlite` | 模拟交易记录 |
| dry_run=false | `tradesv3.sqlite` | 实盘交易记录 |

两个数据库完全独立：
- 模拟盘的交易不会污染实盘记录
- 可以安全地在模拟盘测试新策略
- 表结构完全一致

### 共享数据库方案

如果你用 MySQL/PostgreSQL，建议用不同 database 区分：

```json
// 模拟盘配置
{
    "dry_run": true,
    "db_url": "mysql+pymysql://user:pass@host:3306/freqtrade_dryrun"
}

// 实盘配置
{
    "dry_run": false,
    "db_url": "mysql+pymysql://user:pass@host:3306/freqtrade_live"
}
```

### 注意事项

- dry_run 启动时不会同步交易所订单状态（`startup_update_open_orders()` 被跳过）
- live 启动时会从交易所同步所有未结订单的状态
- 可以通过 `db_url` 手动指定任意数据库

---

## 五、回测模式：无数据库运行

回测模式下**完全不使用数据库**：

```python
# backtesting.py 中的关键设置
Trade.use_db = False
PairLocks.use_db = False
CustomDataWrapper.use_db = False
```

所有数据操作切到内存：
- `Trade.bt_trades` — 内存列表代替数据库查询
- `PairLocks.locks` — 内存列表代替 pairlocks 表
- `CustomDataWrapper.custom_data` — 内存字典代替 trade_custom_data 表

**这就是为什么回测跑完不会在数据库里留下交易记录。**

---

## 六、切换到 MySQL / PostgreSQL

### MySQL

**1. 安装驱动：**
```bash
pip install pymysql
```

**2. MySQL 中创建数据库：**
```sql
CREATE DATABASE freqtrade CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

**3. 修改配置文件：**
```json
{
    "db_url": "mysql+pymysql://用户名:密码@服务器IP:3306/freqtrade"
}
```

**4. 启动 bot（自动建表）：**
```bash
freqtrade trade -c config.json
```

### PostgreSQL

**1. 安装驱动：**
```bash
pip install psycopg2
```

**2. 创建数据库：**
```sql
CREATE DATABASE freqtrade;
```

**3. 配置文件：**
```json
{
    "db_url": "postgresql://用户名:密码@服务器IP:5432/freqtrade"
}
```

### 跨数据库迁移数据

freqtrade 内置了 `convert-db` 命令，可以把 SQLite 的记录迁移到 MySQL：

```bash
freqtrade convert-db \
    --db-url sqlite:///tradesv3.sqlite \
    --db-url-from mysql+pymysql://user:pass@host:3306/freqtrade
```

这会把源数据库中所有表的数据逐行复制到目标数据库，并自动修正自增序列。

---

## 七、数据库迁移

### 自动迁移机制

每次启动 bot 时，`check_migrate()` 自动检测并执行必要的 schema 变更：

| 检测条件 | 执行操作 |
|---------|---------|
| `trades` 表缺少 `record_version` 列 | 备份 → 重建 → 复制数据（处理列名变更如 `sell_reason`→`exit_reason`） |
| `pairlocks` 缺少 `side` 列 | 备份 → 重建 → 默认 `side='*'` |
| `KeyValueStore.key` 长度不足 50 | 重建该表 |
| SQLite WAL 模式未开启 | `PRAGMA journal_mode=wal` |
| 旧版 dry-run 订单状态异常 | 自动修正 close/stop 订单状态 |
| 杠杆交易的 `max_stake_amount` 错误 | 除以杠杆倍数修正 |

### 升级 freqtrade 版本时

无需手动操作——启动新版 freqtrade 时，`check_migrate()` 自动检测并迁移 schema。建议升级前备份数据库文件：

```bash
cp tradesv3.sqlite tradesv3.sqlite.bak
```

---

## 八、常见操作

### 查询交易记录

```bash
# 命令行
sqlite3 tradesv3.sqlite "SELECT id, pair, open_date, close_profit_abs FROM trades WHERE is_open=0 LIMIT 10"

# 或在 Python 中
from freqtrade.persistence import Trade
trades = Trade.get_trades().all()
for t in trades:
    print(f"#{t.id} {t.pair}: profit={t.close_profit_abs}")
```

### 删除某笔交易记录

```bash
freqtrade trade -c config.json
# 然后在 Telegram 中发:
/delete 1    # 删除 trade_id=1 的记录
```

### 清理旧数据

```bash
# 删除 30 天前的已平仓交易
sqlite3 tradesv3.sqlite "DELETE FROM trades WHERE is_open=0 AND close_date < date('now', '-30 days')"
```

### 使用 Navicat / DBeaver 连接

**SQLite：** 直接打开 `tradesv3.sqlite` 文件即可。

**MySQL / PostgreSQL：** 使用配置文件中的连接信息（主机、端口、用户名、密码、数据库名）。

### 查看数据库文件位置

```bash
ls -la *.sqlite
# tradesv3.sqlite        — 实盘记录
# tradesv3.dryrun.sqlite — 模拟盘记录
```

### 监控数据库大小

SQLite 文件会随时间增长，建议定期清理。实测每笔交易大约占用几 KB。如果用 MySQL，可以在数据库管理工具中设置自动清理策略。
